services:
  api:
    build: .
    env_file: .env
    ports:
      - "8290:8000"
    volumes:
      - ./data:/app/data

  ui:
    build:
      context: .
      dockerfile: Dockerfile.ui
    environment:
      GEOFENCE_API_URL: http://api:8000
    ports:
      - "8791:8501"
    depends_on:
      - api
